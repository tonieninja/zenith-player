# Optional frontend flags (Vite). Copy to .env.local if you need them.
# Never commit real secrets.

# In-app QA harness (window.__zenithQA). Dev already enables it.
# VITE_ZENITH_QA=1

# Auto-run after splash: smoke | full | social | release
# VITE_ZENITH_QA_AUTO=
